# ===========================================
# Fulus Pay AI Assistant - Environment Variables
# ===========================================
# Copy this file to .env and fill in your actual values
# Command: cp .env.template .env

# ===========================================
# Database Configuration
# ===========================================
DB_PASSWORD=fulus_password
# Note: DB_URL, DB_USERNAME are set in docker-compose.yml for container networking
# For local development outside Docker, use:
# DB_URL=jdbc:postgresql://localhost:5432/fulus_ai_db
# DB_USERNAME=fulus_user

# ===========================================
# Redis Configuration
# ===========================================
REDIS_PASSWORD=redis_strong_password_here
# Cache TTL in milliseconds (default: 1 hour = 3600000ms)
CACHE_TTL=3600000

# ===========================================
# OpenAI API Configuration
# ===========================================
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI Model Selection (optional, defaults to gpt-4-turbo)
OPENAI_MODEL=gpt-4-turbo
# Alternatives: gpt-4, gpt-3.5-turbo

# ===========================================
# JWT Security Configuration
# ===========================================
# IMPORTANT: Generate a strong secret key for production!
# Must be at least 256 bits (32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Token Expiration Times (in milliseconds)
# Access Token: 24 hours (86400000 ms)
JWT_EXPIRATION=86400000

# Refresh Token: 7 days (604800000 ms)
JWT_REFRESH_EXPIRATION=604800000

# ===========================================
# Application Profile
# ===========================================
# Options: dev, prod
SPRING_PROFILE=dev

# ===========================================
# Optional: Production Configuration
# ===========================================
# Uncomment and configure for production deployment

# SSL Configuration
# REQUIRE_SSL=true

# CORS Allowed Origins (comma-separated)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Server Port (default: 8080)
# SERVER_PORT=8080

# Log Level (default: INFO for prod, DEBUG for dev)
# LOG_LEVEL=INFO

# ===========================================
# Optional: OpenAI Advanced Configuration
# ===========================================
# Temperature (0.0 to 1.0, default: 0.7)
# OPENAI_TEMPERATURE=0.7

# Max Tokens per response (default: 1000)
# OPENAI_MAX_TOKENS=1000

# ===========================================
# Optional: Chat Memory Configuration
# ===========================================
# Maximum messages to keep in memory (default: 20)
# CHAT_MEMORY_MAX_MESSAGES=20

# Cache TTL in seconds (default: 3600 = 1 hour)
# CHAT_MEMORY_CACHE_TTL=3600

# Message retention days (default: 30)
# CHAT_MEMORY_RETENTION_DAYS=30
